import { portfolioItem } from './portfolioItem';
import { heroSection } from './hero';
import { capabilitySchema } from './capability';

export { portfolioItem, heroSection, capabilitySchema };
export const schemaTypes = [portfolioItem, heroSection, capabilitySchema];
